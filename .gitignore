node_modules/
.project_id
.github_repo
.env
.env.*
__pycache__/
*.pyc
backend/data/
.DS_Store
dist/
.output/
.vercel/
.nitro/
.pytest_cache/

backend/docs/*.pdf
# Sight drops photos here; GITHUB.md rules out committing identity shots.
backend/docs/learn/shots/
# ...and the model's reading of a photo discloses the same thing in words.
backend/docs/learn/sight/

# Client vault and anything that identifies a client. The desk keeps these
# outside the repo by default (FORTITUDO_DATA_ROOT), but a local override can
# point them back inside it, and one commit is all it takes.
backend/docs/clients/
FortitudoData/
desk-token.txt
*.db
*.sqlite
Modelfile.built
identity.json
